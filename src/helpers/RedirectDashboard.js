import { React, Component } from 'react';

export function rutaDelDashboardParaElRol(rol) {
    if (rol === 'apoderado') {
        return "/apoderado"
    }
    else if (rol === 'empleado-mesa-de-entradas') {
        
    }
}